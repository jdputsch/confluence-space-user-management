                ## Provide pagerAction links for users

                #if ($action.selectedGroup)
                    #if($action.hasPrev($users))
                        <a href="docustompermissionsmanage.action?key=${key}&selectedGroup=${action.selectedGroup}&pagerAction=prevPageUsers">&lt;</a>
                    #end
                    #if($action.hasNext($users))
                        <a href="docustompermissionsmanage.action?key=${key}&selectedGroup=${action.selectedGroup}&pagerAction=nextPageUsers">&gt;</a>
                    #end
                #end